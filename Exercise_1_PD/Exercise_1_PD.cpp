#include <iostream>
using namespace std;

double ratarata(double a, double b) {
	return (a + b) / 2;
}

string status(double t, double m) {
	if (t = 70 || m = 80)
		return "Diterima";
	else
		return "Di tolak";
}